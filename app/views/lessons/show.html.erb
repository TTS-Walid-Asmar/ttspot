<h1>Lesson # <%= @lesson.number %></h1>
<h1><%= @lesson.title %></h1>
<div class="well">
  <%= markdown(@lesson.entry) %>
</div>

<%= link_to "Edit", edit_lesson_path(@lesson) %> |
<%= link_to "Back to Course", course_path(@lesson.course) %>
